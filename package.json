{
  "name": "salesforce-reporter-api",
  "license": "MIT",
  "version": "1.0.0",
  "description": "",
  "private": "true",
  "npmClient": "yarn",
  "main": "server.js",
  "scripts": {
    "start": "yarn compile:ts && NODE_ENV=stable node ./packages/app/dist/server.js"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
