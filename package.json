{
  "name": "salesforce-reporter-api",
  "license": "MIT",
  "version": "1.0.0",
  "description": "",
  "private": "true",
  "main": "server.js",
  "scripts": {
    "start:local": "yarn build && node ./packages/server/build/index.js",
    "start": "yarn build && node ./packages/server/build/index.js",
    "test": "yarn lerna run test",
    "build": "yarn lerna run build",
    "watch:dev": "nodemon -L --exec babel-node  ./packages/server/src/index.js"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "nodemon": "^2.0.4"
  },
  "dependencies": {
    "@babel/cli": "^7.10.5",
    "@babel/core": "^7.11.1",
    "@babel/node": "^7.10.5",
    "@babel/preset-env": "^7.11.0",
    "babel-plugin-module-resolver": "^4.0.0",
    "core-js": "2",
    "lerna": "^3.22.1",
    "pg": "^8.6.0"
  }
}
